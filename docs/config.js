// Deployment-specific settings. Filled in once the push worker is deployed;
// the notification button stays hidden while pushWorkerUrl is empty.
window.PULSE_CONFIG = {
  pushWorkerUrl: "",
  vapidPublicKey: ""
};
